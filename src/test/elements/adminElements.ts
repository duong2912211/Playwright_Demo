import { expect, Page } from "@playwright/test";
import BaseElements from "./baseElements";

export default class AdminElements extends BaseElements {
    constructor(page: Page) {
        super(page);
    }

    
}